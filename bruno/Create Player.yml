info:
  name: Create Player
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/players"
  auth: inherit
  body:
    mode: json
    json: |
      {
        "username": "nraboy"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
